안녕하세요!
경북산업직업전문학교 -AI를 활용한 빅데이터 플랫폼 개발자 1차반 수강생, 이기정의 프로젝트들을 담는 Git-hub 저장소입니다.

- 업데이트 된 목록 -

1. DATABASE CRUD PROJECT_이기정
	프로젝트명 : 	JAVA와 DB의 연결 후 프로시저 호출을 통해 CRUD의 실행
	프로젝트 기간 : 	2021.03.11~12, 17.
	프로젝트 목표 : 	1. DB에서 CRUD(조회, 추가, 수정, 삭제)를 실행하는 프로시저를 생성할 수 있다.
		    	2. JAVA에서 DB의 연결을 실행할 수 있다.
			3. JAVA에서 프로시저를 호출할 수 있다.
			4. JAVA에서 프로시저를 호출하여 DB에 데이터를 추가,수정,삭제할 수 있다.
			5. JAVA에서 프로시저를 호출하여 DB에 데이터를 조회할 수 있다.
	프로젝트 내용 : 	Oracle SQL Developer를 활용해 SQL문을 통하여 Student 테이블을 생성하고,
			여러가지 SQL질의를 통해 데이터베이스를 구축하였습니다.
			CRUD(조회, 추가, 수정, 삭제)에 해당하는 프로시저를 생성한 후, JAVA를 이용하여
			DB를 연결해보았으며, 만들어 놓은 각 프로시저를 사용하는 실습을 해 보았습니다.